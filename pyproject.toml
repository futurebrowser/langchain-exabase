[project]
name = "exabase-workspace"
version = "0.1.0"
description = "Exabase monorepo"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.uv]
package = false

[tool.uv.workspace]
members = ["packages/*"]

[tool.ruff]
line-length = 100
