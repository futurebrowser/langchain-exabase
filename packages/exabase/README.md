# Exabase Client

Reusable Python client for the Exabase API.

## Usage

Use it from the workspace:

```bash
uv run --package exabase python -c "from exabase import Exabase; print(Exabase)"
```

## Exports

- `Exabase`
- `ExabaseError`
